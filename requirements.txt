### AgriFlow Engine — Python Dependencies
### Tested with Python 3.12, kompatibel dengan 3.10+
###
### Versions pinned exact (==) for reproducible HF Spaces builds.
### To bump: upgrade locally, run the full test suite, then update here.

# Core scientific
scipy==1.17.1
numpy==2.4.4

# Algorithm libraries
matching==1.4.3          # Gale-Shapley stable matching (Tier 1)

# Geospatial
geopy==2.4.1             # Geodesic distance fallback

# Testing
pytest==9.0.3

# Data source connectors (HTTP)
requests==2.33.1
beautifulsoup4==4.14.3   # HTML parsing untuk PIHPS scraper

# API server — required by whatsapp_bot/
fastapi==0.136.1
pydantic==2.13.4
uvicorn==0.47.0
httpx==0.28.1            # required by fastapi.testclient

# WhatsApp bot (M2 milestone — Twilio webhook + Gemini RAG)
twilio==9.10.9
google-genai==2.3.0      # NEW SDK (google-generativeai deprecated 2025)
python-dotenv==1.2.2
python-multipart==0.0.28 # FastAPI form parsing for Twilio webhook

# Database layer (Step 1 — Supabase/Postgres backend; gated by DATA_BACKEND=postgres)
sqlalchemy==2.0.41       # ORM / core — used by db/db_loader.py
psycopg2-binary==2.9.10  # Postgres adapter for SQLAlchemy
supabase==2.15.2         # Supabase Python client (auth, storage, RPC — future use)
